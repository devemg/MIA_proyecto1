#include <stdio.h>

void analizarComando(char[]);

void limpiar(char[]);

int isLetra(char);

int isNumero(char);

int evaluarComandos(char[],int*,int*);

int evaluarParametros(char[],int,int*,int*);

void reporte(int,char[],char[],char[],char[],char[],char[],char[]);

void toLowerCase(char*);
